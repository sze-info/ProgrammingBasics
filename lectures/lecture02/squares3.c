#include<stdio.h>
int main(void) {
  int base;
  printf("Squares of natural numbers\n\n");
  base = 1;
  printf("%d^2=%d\n", base, base*base);
  base = base + 1;
  printf("%d^2=%d\n", base, base*base);
  base = base + 1;
  printf("%d^2=%d\n", base, base*base);
  base = base + 1;
  printf("%d^2=%d\n", base, base*base);
  base = base + 1;
  printf("%d^2=%d\n", base, base*base);
  base = base + 1;
  printf("%d^2=%d\n", base, base*base);
  base = base + 1;
  printf("%d^2=%d\n", base, base*base);
  base = base + 1;
  printf("%d^2=%d\n", base, base*base);
  base = base + 1;
  printf("%d^2=%d\n", base, base*base);
  base = base + 1;
  printf("%d^2=%d\n", base, base*base);
  return 0;
}
